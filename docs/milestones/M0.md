# M0 — 安装 Godot 4 引擎

**状态**：✅ done
**commit**：`7818be2`（前后相关）
**日期**：2026-07-04

## 任务
- [x] 下载 Godot 4.3 stable Mac universal zip（100MB）
- [x] 解压到 `~/Applications/Godot.app`（237MB）
- [x] `open ~/Applications/Godot.app` 启动成功

## 验证
- Godot Project Manager 打开正常
- 二进制含 `WebSocketPeer` 类（不含 `WebSocketClient`，已确认走手写 WS 协议路径）

## 备注
- 4.3 stable 这个 binary 的 web 模块只 export 了 `WebSocketPeer` 基类，未 export `WebSocketClient`
- M1 的 `ws_client.gd` 改为手写 RFC 6455（StreamPeerTCP + 握手 + frame 解析）
