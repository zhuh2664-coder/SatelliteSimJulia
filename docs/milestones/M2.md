# M2 — WebSocket 端到端（list + start + frame stream）

**状态**：✅ done
**验证方式**：headless 脚本 `/tmp/ws_e2e.gd`（临时，不入仓）

## 任务
- [x] 验证 WS 握手成功
- [x] 验证收到 `list_constellations_response`（14 个星座）
- [x] 验证 `start_simulation` → `start_simulation_response`（66 sats × 7 frames）
- [x] 验证 `frame` 推流：7 帧、198 位置/帧、132 ISL 候选、93 可用
- [x] 验证 `stream_end` 收到

## 结果
```
OPEN
list: 14 constellations
start: n_sat=66 n_time=7
frame1: t=0 positions=198 pairs=132 avail=93
FINAL: opened=true start_resp=true frames=7
```

## 数据一致性
与 Julia 端 `scripts/e2e_client.jl` 验证结果一致：
- 91-93/132 ISL 可用
- 198 位置（66×3）
- 7 帧完整

## 备注
- M2 不需 Godot GUI
- 完整可视化要等 M3+
