# M1 — Godot 项目骨架

**状态**：✅ done
**commit**：`da7cb75`（snapshot 含主结构） + `7818be2`（手写 WS 协议）

## 任务
- [x] `godot-sandbox/project.godot` — autoload `WsClient` + main scene
- [x] `godot-sandbox/scenes/main.tscn` — Camera3D + DirectionalLight3D + UI 节点
- [x] `godot-sandbox/scripts/ws_client.gd` — RFC 6455 手写 WS 客户端
- [x] `godot-sandbox/scripts/sandbox_world.gd` — 3D 地球/卫星/ISL 节点
- [x] `godot-sandbox/scripts/sandbox_ui.gd` — uGUI 面板
- [x] `godot-sandbox/scripts/game_bootstrap.gd` — 总控（消息路由 + 播放）
- [x] `godot-sandbox/icon.svg` + `README.md` + `.gitignore`

## 验证
- `godot --headless --check-only` 编译通过
- 手写 WS client 在 headless 测试中能连 Julia 服务

## 备注
- Godot 4.3 stable 缺 `WebSocketClient` 类
- WS 协议实现 ~200 行，覆盖握手 + text frame 解析 + masked frame 发送
