# M4 — ISL 实时连线

**状态**：⏳ pending
**owner**：用户自己实现

## 任务
- [ ] 通读 `update_frame()` 中 ISL 渲染部分
- [ ] iridium Start 后看到 93 绿 + 39 灰
- [ ] 修任意你看着不爽的地方

## 验收
- 132 条线在 3D 视图显示
- 可用变绿、不可用变灰
- 端点贴着卫星球

## 自由发挥（可选）
- 可用边发光/脉动（用 `Tween` 做 alpha 动画）
- 不可用边变细 / 透明
- 按距离染色（近红远蓝）

## 我能帮你
- `ImmediateMesh` API
- `Tween` 动画示例
- 性能优化（>500 边时换 `MultiMeshInstance3D`）
