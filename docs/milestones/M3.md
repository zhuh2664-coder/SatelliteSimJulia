# M3 — 3D 地球 + 卫星散点

**状态**：⏳ in progress
**owner**：用户自己实现

## 任务
- [ ] 通读 `godot-sandbox/scripts/sandbox_world.gd` 的 `init()` / `update_frame()`
- [ ] 启动 Julia 后端 + Godot ▶ Play
- [ ] 选 `iridium` → Start
- [ ] 看到地球（蓝色球）+ 66 颗卫星（橙色点）

## 验收
- 地球居中、半径合适
- 卫星点大小合适（0.4 units）和颜色（橙色）
- 不需要动，但能正确显示

## 自由发挥（可选）
- 加地球贴图（放 `godot-sandbox/textures/earth.jpg`，在 `sandbox_world.gd` 加载）
- 改卫星大小/颜色
- 加 Camera 略斜视而非正俯视
- 改地球大小

## 我能帮你
- API 用法（`SphereMesh`, `StandardMaterial3D` 等）
- 调试（Editor Log 报错截图给我）
- 性能建议（>500 颗时考虑 MultiMeshInstance3D）
