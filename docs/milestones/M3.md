# M3 — 3D 地球 + 卫星散点

**状态**：✅ done
**commits**：`28572c9`, `5716614`, `356d5fe`

## 任务
- [x] 通读 `godot-sandbox/scripts/sandbox_world.gd`
- [x] 启动 Julia 后端 + Godot ▶ Play
- [x] 选 `iridium` → Start
- [x] 看到地球（蓝色球）+ 66 颗卫星（橙色点）

## 完成步骤（一个微改一个 commit）

| ID    | 改动                                          | commit    |
|-------|-----------------------------------------------|-----------|
| M3.1  | 抽出 `ecef_km_to_unity()` 静态 helper          | `28572c9` |
| M3.2  | 拆 `init()` 为 `_build_earth/_satellites/_isl_lines` | `5716614` |
| M3.3  | `SCALE/颜色` 改 `@export var`（Inspector 可调）| `356d5fe` |

## 验收
- 地球居中、半径 63.78 units
- 卫星点 0.4 units 半径、橙色发光
- ECEF → Godot 坐标变换正确（北极朝上、0°经度朝 X）
- 三个微改每次 e2e 都对：first sat pos = (-3208.22, 6390.92, 0) km

## 自由发挥（未做）
- 加地球贴图（`godot-sandbox/textures/earth.jpg`）
- 改卫星大小/颜色
- 加 Camera 略斜视而非正俯视
- 改地球大小（@export 已可调）
