name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  smoke:
    name: smoke-core
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Julia
        uses: julia-actions/setup-julia@v2
        with:
          version: '1.12'

      - name: Cache Julia artifacts
        uses: julia-actions/cache@v2

      - name: Instantiate core project
        run: julia --project=src/core -e 'using Pkg; Pkg.instantiate()'

      - name: Core smoke test
        run: julia --project=src/core test/smoke.jl
